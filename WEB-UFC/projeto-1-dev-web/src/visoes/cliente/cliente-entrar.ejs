<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="http://localhost:3000/css/bootstrap/bootstrap.min.css" rel="stylesheet" />

    <style>
    body{
        background-color: RGBA(33,37,41,var(--bs-bg-opacity,1));
        font-family: var(--bs-body-font-family);
    }
    .container {
        width: 400px;
        margin: 8% auto;
        padding: 20px;
        background-color: #f2f2f2;
        border-radius: 5px;
    }

    h1 {
        text-align: center;
        font-size: 32px;
    }

    label {
        display: block;
        margin-bottom: 10px;
    }

    input{
        width: 100%;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
    }

    button {
        width: 100%;
        padding: 10px;
        margin-top: 10px;
    }
    p{  
        margin: 0;
        text-align: center;
        padding: 5px;
    }
    link-cadastrar{
        display: block;
        text-align: center;
    }
    </style>
    <title>Entrar</title>
</head>
<body>
    
    <div class="container">
        <form action="/signin" method="POST">
        <h1 class="mb-4">Entrar</h1>

        <% if (erros && erros.length > 0) { %>
          
          <% erros.forEach(function(erro) { %>
            <div class="alert alert-danger w-100" role="alert">
              <%= erro %>
            </div>
          <% }); %>

        <% } %>

        <br> 
        <label>Email:</label>
        <input type="email" name="email">
        <br>
        <label>Senha:</label>
        <input type="password" name="senha">
        <button type="submit" class="btn btn-warning">Entrar</button>
        <p>Ou</p>
        <br>
        
     </form><a class="link-cadastrar" href="/signup">Cadastre-se</a>
    </div>

    <script src="http://localhost:3000/js/bootstrap/bootstrap.bundle.min.js"></script>
</body>
</html>